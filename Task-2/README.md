Task 2 - Using .gitignore and tracking files

Objective:
    1. Create a .gitignore file
    2. Add ignore rules (exclude .log and temp.txt files)
    3.Verigy using git status

1. Create '.gitignore' file and add ignore rules
        1.*.log
        2. temp.txt

2. Verify using git status

        git status

3. Add files to GitBub

        git add .gitignore main.txt
        git commit -m "Added .gitignore and main.txt"
        git push
        